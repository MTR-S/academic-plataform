server:
  port: 8083

spring:
  application:
    name: assignment-service
  data:
  mongodb:
    host: localhost
    port: 27017
    database: assignments
    username: ${MONGO_INIT_USER}
    password: ${MONGO_INIT_PASSWORD}
    authentication-database: admin

jwt:
  secret: ${JWT_SECRET}